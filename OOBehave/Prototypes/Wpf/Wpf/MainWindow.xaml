<Window x:Class="Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>

        <TextBlock>First Name</TextBlock>
        <TextBox Text="{Binding Path=FirstName, Mode=TwoWay, ValidatesOnDataErrors=True}"></TextBox>

        <TextBlock>Last Name</TextBlock>
        <TextBox Text="{Binding Path=LastName, Mode=TwoWay, ValidatesOnDataErrors=True}"></TextBox>

        <TextBlock>Short Name</TextBlock>
        <TextBlock Text="{Binding Path=ShortName, Mode=OneWay}"></TextBlock>

        <TextBlock>Title</TextBlock>
        <TextBox Text="{Binding Path=Title, Mode=TwoWay, ValidatesOnDataErrors=True}"></TextBox>

        <TextBlock>Full Name</TextBlock>
        <TextBlock Text="{Binding Path=FullName, Mode=OneWay}"></TextBlock>

    </StackPanel>
</Window>
